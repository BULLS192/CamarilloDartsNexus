name: V0.11.2 Render Build 2
on:
  push:
    branches:
      - diagnostic/v0112-render-build-2
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build exact Render Docker image
        run: docker build --progress=plain -t camarillo-v0112 .
