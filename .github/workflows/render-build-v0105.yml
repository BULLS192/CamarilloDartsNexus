name: Render Build V0.10.5
on:
  push:
    branches: [diagnostic/v0105-render-build]
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build exact Render Docker image
        run: docker build --progress=plain -t camarillo-v0105 .
