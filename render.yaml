services:
  - type: web
    name: camarillo-darts
    runtime: docker
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
