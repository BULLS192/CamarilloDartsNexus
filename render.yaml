services:
  - type: web
    name: camarillo-darts
    runtime: docker
    plan: free
    region: ohio
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
